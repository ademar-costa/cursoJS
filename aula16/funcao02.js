function soma(n1=0, n2=0){// se os parametros não for passado, ele vai colocar o valor zero atribuido a ela dentro da function 
    return n1 + n2
}
console.log(soma(7)) // aqui eu não declarei o segundo atributo só o primeiro 
console.log(soma(2, 8))